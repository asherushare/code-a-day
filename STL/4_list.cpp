#include <bits\stdc++.h>
using namespace std;

void explainList()
{
  list<int> ls;

  ls.push_back(2);
  ls.emplace_back(8);

  ls.push_back(7);

  ls.emplace_back(90);
}

int main() {


  return 0;
}